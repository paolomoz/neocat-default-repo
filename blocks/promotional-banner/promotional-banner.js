export default function decorate(block) {
  // Block structure is already correct from AEM
  // No additional JavaScript manipulation needed
  // The CSS handles all styling using structural selectors
}